🧩 Mod Name

Loadout

Tagline (internal):
Smart hotbar, armor, and offhand management with fully customizable rules.

📁 Folder Structure (Fabric · Client-side)
loadout/
├─ build.gradle
├─ settings.gradle
├─ gradle.properties
├─ gradlew
├─ gradlew.bat
├─ gradle/
│  └─ wrapper/
│     ├─ gradle-wrapper.jar
│     └─ gradle-wrapper.properties
│
└─ src/
   └─ main/
      ├─ java/
      │  └─ com/
      │     └─ loadout/
      │        ├─ LoadoutClient.java        # Fabric client initializer
      │        ├─ LoadoutConfig.java        # Cloth Config schema
      │        ├─ SlotRoutingConfig.java    # Per-slot routing configuration
      │        ├─ ItemRouter.java           # Real-time item routing logic
      │        ├─ ItemMatcher.java          # Item category matching
      │        ├─ CategoryMapper.java       # Item to category mapping
      │        ├─ ItemSwapper.java          # Inventory manipulation
      │        ├─ EventListener.java        # Pickup hooks
      │        └─ ui/
      │           └─ LoadoutConfigScreen.java
      │
      └─ resources/
         ├─ fabric.mod.json
         └─ assets/
            └─ loadout/
               ├─ lang/
               │  └─ en_us.json
               └─ icon.png

⚙️ Target & Constraints

Minecraft versions: 1.20.10+ ONLY

Mod loader: Fabric

Environment: Client-side only

Dependencies:

Fabric API

Cloth Config

(Optional) Mod Menu

No server-side logic

No gameplay advantage beyond automation

No default behavior enabled

🧠 Core Behavior Summary

Loadout automatically:

Routes items to designated slots in real-time

…based entirely on user-defined rules, triggered when:

Items are picked up (immediate routing)

A manual hotkey is used

🎛️ Customization Rules (High-Level)

Each slot is independently configurable:

Item category assignment (Weapon, Tool, Armor, Food, etc.)

Replacement mode (Never, Same Type Only, Always)

Slot locking

🖥️ Configuration UI Requirements

Built using Cloth Config

Slot-based editor

Per-slot rule toggles

Sensible defaults for new users

All behavior is opt-in

Changes apply immediately

Clear visual layout matching the hotbar

🛡️ Safety & UX Rules

Minimal safety checks:

No routing when player is dead

No routing in spectator mode

Manual override keybind

Never delete or duplicate items

🧠 MASTER PROMPT (Clean & Final)

Copy exactly as-is:

You are an expert Minecraft Fabric mod developer.

Create a complete, production-ready Fabric CLIENT-SIDE mod named “Loadout” for Minecraft versions 1.20.10 and above.

MOD PURPOSE:
Loadout automatically routes items to designated slots in real-time based on fully user-defined rules whenever items are picked up.

SCOPE & CONSTRAINTS:
- Client-side only
- Fabric API
- Minecraft 1.20.10+
- Cloth Config for all configuration
- No server-side code
- Sensible defaults for new users

ROUTING SYSTEM:
- Real-time item routing
- Each slot is independently configurable
- Slot rules include:
  - Item category assignment (Weapon, Tool, Armor, Food, etc.)
  - Replacement mode (Never, Same Type Only, Always)
  - Slot locking

EVENT TRIGGERS:
- Item pickup (immediate routing)
- Manual hotkey activation

CONFIGURATION UI:
- Built with Cloth Config
- Slot-based editor
- Per-slot rule toggles
- No presets or forced defaults
- Changes apply instantly

UX & SAFETY:
- Do not route items when player is dead or in spectator mode
- Never destroy or duplicate items

DELIVERABLES:
- Complete source code
- Proper Fabric project structure
- fabric.mod.json
- Clean, commented implementation
- Buildable project with no placeholder logic

QUALITY BAR:
The mod should feel minimal, polished, and professional — focused purely on quality-of-life improvements.
